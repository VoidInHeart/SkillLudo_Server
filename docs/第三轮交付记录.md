# 第三轮交付记录

## 阶段一 · 2026-09-13

击落按落点顺序包含跳跃/虫洞两端；跳跃起跳格不再漏判，普通途经格不受影响。无击落救援与零步动作仍不击落。

验证：82 项测试通过。L05 覆盖四阵营普通跳跃、先跳后虫洞、先虫洞后跳及禁止击落；L06 验证攻击方可在后续回合离开锁定法国，法国等格子清空后用 3/4 解锁。导出三种攻击方与锁定法国同格的权威快照供前端真实点击回归。

## 阶段二 · 2026-09-13

新增认证账号投稿、JSON 形式化、MySQL 持久化与去重、SMTP 465 TLS 发送。复用现有进程及连接池。88 项后端回归、协议同步、编译及真实 MySQL 集成验证通过。生产投稿表与专用 Secret 已准备，SMTP 认证通过，未发送外部测试邮件。CI 增加投稿数据库集成，并在镜像发布同次 rollout 引用 SMTP Secret。配置和恢复说明见 `国家投稿与反馈.md`。

发布复核补齐 Docker COPY 与 .dockerignore 白名单，使 `scripts/verify-smtp.mjs` 一并进入运行镜像，便于容器内按文档自检。投稿应用逻辑不变。

## 2026-09-14 上线验收

后端最终镜像 `9982a3d0111063fdbf9576f0de99a9568cc2175f` 已部署，配套客户端 `d1be5984990a4d2e31aa4d9fe526c69aec584fe4`。CI `34749564025` 成功，包含 88 项服务端与 22 项客户端回归、真实 MySQL 及受限 SSH 自动部署；前端 CI `34749407422` 成功。

生产 readyz 正常，三个 Pod 1/1、零重启。实际容器内执行 SMTP 自检通过；以同一应用数据库权限验证账号关联、中文 JSON、去重、限流和隔离通过，测试账号已清理，邮件使用替身。公网真实双人对局通过，浏览器零错误。SMTP 授权码仅存服务器 Secret，无需 GitHub 新 Secret。没有外部测试邮件或收件箱验收。功能与部署收尾完成。

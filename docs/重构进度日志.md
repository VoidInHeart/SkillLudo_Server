# 服务端重构日志

2026-09-08 · S0：基线 12 项测试通过，工作树干净。计划实现双骰选择状态、颜色意愿抽签、禁用快速匹配、技能扩展接口，并按原棋盘修正拓扑。保持服务端权威与前后端分仓。

跨仓阶段计划见 `../../docs/重构进度日志.md`。下一步：S1 权威协议与规则；同时生成可在客户端校验的协议及逻辑映射镜像。

## S1 完成

- 协议 v2：`ROLL_DICE → WAIT_SELECT_DIE → SELECT_DIE {dieIndex, rollId} → WAIT_SELECT_PIECE`。服务端生成双骰，选择值决定起飞/额外回合；托管按阶段运行，重连快照完整包含两个骰子。
- 私人房间颜色意愿，冲突公平抽签，剩余颜色和玩家随机配对；修改意愿取消准备。快速匹配返回 `MATCHMAKING_DISABLED`，未来 `MATCHMAKING` 分配模式忽略意愿。
- 拓扑与原图对齐：起飞 0，主环 1–50，跑道 51–56；同色为 2 mod 4；18→30 为飞行通道，支持 14→18→30。飞行移动段不再沿普通环逐格播放。
- 快照由服务器生成移动预览，客户端后续移除重复规则。新增移动段与撞机位置事件，为 3D 动画提供明确输入。
- `FactionSkills.ts`、技能命令、窗口、状态接口预留；默认所有阵营技能为空，未启用技能不可执行。
- `scripts/sync-client-contract.mjs [--check]` 镜像协议和拓扑；保持双仓但检测契约漂移。
- 验证：19 项测试通过，覆盖全部 625 种颜色意愿组合、50 场种子 AI 对局、双骰非法/重复/过期选择、终点与虫洞；TypeScript 检查及 build 通过。
- 协议 v2 与旧客户端不兼容；前后端需成对更新，旧内存棋局不迁移。

## S3：联机校验补齐（2026-09-09）

- `SELECT_PIECE` 增加当前 `rollId` 检查，拒绝旧确认窗口或迟到移动命令。
- 加入真实 WebSocket 的双骰选择、颜色意愿、重连和过期移动拒绝测试，服务端累计 20 项测试通过。
- 移除已经禁用的旧快速匹配实现；保留的匹配查找仅搜索 `MATCHMAKING` 房间，避免未来开放时混入私人房间。
- 提供不依赖 MySQL 的独立联机验收服务 `npm run verify:server`，默认端口 3101；正常服务仍按原账号/数据库入口启动。
- 客户端真实浏览器已验证开房、颜色意愿、双方准备、开局、双骰投掷、点击选点、确认起飞和选骰阶段重连。
- S4 下一步：最终契约/构建检查、文档和双仓恢复点索引。

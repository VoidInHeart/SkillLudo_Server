# 服务端重构日志

2026-09-08 · S0：基线 12 项测试通过，工作树干净。计划实现双骰选择状态、颜色意愿抽签、禁用快速匹配、技能扩展接口，并按原棋盘修正拓扑。保持服务端权威与前后端分仓。

跨仓阶段计划见 `../../docs/重构进度日志.md`。下一步：S1 权威协议与规则；同时生成可在客户端校验的协议及逻辑映射镜像。

## S1 完成

- 协议 v2：`ROLL_DICE → WAIT_SELECT_DIE → SELECT_DIE {dieIndex, rollId} → WAIT_SELECT_PIECE`。服务端生成双骰，选择值决定起飞/额外回合；托管按阶段运行，重连快照完整包含两个骰子。
- 私人房间颜色意愿，冲突公平抽签，剩余颜色和玩家随机配对；修改意愿取消准备。快速匹配返回 `MATCHMAKING_DISABLED`，未来 `MATCHMAKING` 分配模式忽略意愿。
- 拓扑与原图对齐：起飞 0，主环 1–50，跑道 51–56；同色为 2 mod 4；18→30 为飞行通道，支持 14→18→30。飞行移动段不再沿普通环逐格播放。
- 快照由服务器生成移动预览，客户端后续移除重复规则。新增移动段与撞机位置事件，为 3D 动画提供明确输入。
- `FactionSkills.ts`、技能命令、窗口、状态接口预留；默认所有阵营技能为空，未启用技能不可执行。
- `scripts/sync-client-contract.mjs [--check]` 镜像协议和拓扑；保持双仓但检测契约漂移。
- 验证：19 项测试通过，覆盖全部 625 种颜色意愿组合、50 场种子 AI 对局、双骰非法/重复/过期选择、终点与虫洞；TypeScript 检查及 build 通过。
- 协议 v2 与旧客户端不兼容；前后端需成对更新，旧内存棋局不迁移。

## S3：联机校验补齐（2026-09-09）

- `SELECT_PIECE` 增加当前 `rollId` 检查，拒绝旧确认窗口或迟到移动命令。
- 加入真实 WebSocket 的双骰选择、颜色意愿、重连和过期移动拒绝测试，服务端累计 20 项测试通过。
- 移除已经禁用的旧快速匹配实现；保留的匹配查找仅搜索 `MATCHMAKING` 房间，避免未来开放时混入私人房间。
- 提供不依赖 MySQL 的独立联机验收服务 `npm run verify:server`，默认端口 3101；正常服务仍按原账号/数据库入口启动。
- 客户端真实浏览器已验证开房、颜色意愿、双方准备、开局、双骰投掷、点击选点、确认起飞和选骰阶段重连。
- S4 下一步：最终契约/构建检查、文档和双仓恢复点索引。

## S4：最终验证与文档完成（2026-09-09）

- S1 基线 `b17b520`，S3 联机补齐 `bd0b7bb`；配套客户端 S2 `d4ed194`、S3 `38a4c2e`。
- 最终 `npm test`（20 项）、`npm run check`、`npm run build`、`npm run contract:check` 全部通过。协议 v2 与客户端镜像完全一致。
- 客户端真实 Chrome 双连接完成选色、准备/开局、双骰、实体点击起飞和选骰重连；Web 和微信构建成功。客户端验收结果保存在其 `docs/verification`。
- README 已更新 v2 状态机、`rollId`、棋盘拓扑、最大意愿满足算法、技能接口、匹配开关、同步契约和独立验证服务。
- 本提交为文档收尾恢复点。没有推送或部署服务器，没有迁移旧内存对局；发布前成对更新客户端并接入真实微信身份校验/WSS。

## R1：补充回归测试（2026-09-09）

- 新增 `tests/Regression.test.ts` 的 S01–S08：第二枚骰子语义、旧 rollId 不改状态、快照/返回值隔离、虫洞预览与实际吃子、私人位置不误吃、拒绝非法房间操作、重开对局、匹配隔离。
- 新增 `tests/ProtocolRegression.test.ts` 的 P01–P03：真实 WebSocket 重传幂等、待选骰/待选飞机时替换连接。通过其他玩家先观察状态，防止重连清除 AI 标志而掩盖旧连接关闭问题。
- 新增测试类型配置与 `npm run check:tests`。服务端累计 31 项测试全部通过，应用/测试 TypeScript 与契约镜像检查通过；无业务源码改动。
- 工作区级 README 和测试文档位于 `../../docs`；合计 43 项自动化通过。本轮未重新执行构建、浏览器或微信设备验收。

## R2：本机数据库与后台启动（2026-09-09）

- 按用户授权配置本机 `skill_ludo` 数据库、`skillludo_app` 应用账号，验证应用账号可以连接且 4 张业务表存在。实际密码仅保存到忽略的 `.env`；临时 root 凭据文件与一次性配置脚本已删除。
- 新增仓库根目录 `start.ps1`、`start.sh`，共用 `scripts/start-local.mjs`：按脚本位置读取配置、编译、默认后台启动、记录日志/PID、等待端口就绪；支持可选前台运行，重复启动报告已有实例。
- `.runtime/` 和 `.local/` 已忽略；Shell 文件固定 LF。修复当前 Git Bash 环境缺少 `dirname` 的问题，定位项目仅使用 Shell 内建命令。
- 已验证：PowerShell/JavaScript/Shell 语法、非法参数拒绝、TypeScript 编译、PowerShell 从工作区根目录后台启动、启动命令退出后真实 WebSocket `AUTH_OK`/`PONG`、Shell 与 PowerShell 重复调用均保持同一 PID；启动错误日志为空。
- 后端保留在本机后台运行，默认 `ws://127.0.0.1:3000`。启动脚本不包含密码、不设置开机启动或崩溃重启。用法、停止方法和日志位置见仓库 README 与工作区 `../../docs/README.md`。
- 本轮没有更改游戏业务源码，未重跑此前 43 项游戏回归、Cocos 构建或微信真机；Shell 执行验证使用 Windows Git Bash，Linux/macOS 原生环境和前台模式未实测。

## D1：k3s 部署与 CI/CD 实现（2026-09-09）

- 目标主机通过 `ssh SEBaseline` 访问，Ubuntu 24.04，4 vCPU、3659 MiB 内存。按用户授权停用旧 `edu-platform-prod` 的 3 个 Deployment 与 2 个 StatefulSet；其 PV 保留，MySQL/MinIO 停机后冷备份与恢复配置保存在 `/opt/skillludo/backups`。
- 新增 Node 24 容器、独立 `skillludo` 命名空间、MySQL/校准 PVC、NetworkPolicy 与 Traefik 公网 IP 入口。游戏服务单副本 Recreate；部署会中断内存棋局，不能宣称零停机或跨副本恢复。
- 新增 `/healthz`、带数据库检查的 `/readyz`、5 秒有界关闭，以及 16 KiB WebSocket 消息上限。新增 D01–D03，当前服务端 34 项测试及应用/测试类型检查通过。
- 新增真实双连接发布冒烟检查与四连接一房间的可复现压测工具。GitHub CI 执行服务端/配套客户端测试与契约检查、构建按提交标记的容器，main 经 production 环境上传镜像给受限 SSH 接收器；失败会尝试恢复上个镜像。
- GitHub production 已配置 `DEPLOY_SSH_KEY`、`DEPLOY_KNOWN_HOSTS` 两个 Secret 和 `DEPLOY_HOST`、`DEPLOY_USER` 两个 Variable；仅允许 main 分支部署。服务器专用密钥不能执行普通命令。所有真实凭据均未进入仓库。
- 本恢复点为实现阶段。旧业务已停止；新服务上线、GitHub 工作流实跑和容量结果仍需后续验证记录。数据库沿用主机已缓存的 MySQL 8.4，避免将导入后的本地 manifest 摘要误当作公开 registry 摘要。

## D2–D3：发布成功与容量评估（2026-09-09）

- D1 `9e1350e` 已推送。首轮完整镜像跨境传输过慢，改为从 CI 构建的镜像提取约 1.02 MB 运行包，在服务器使用固定模板和缓存 Node 基础镜像组装容器。D2 `2634595` 的 GitHub 工作流 `34331813657` 全部通过，构建约 28 秒、部署约 70 秒。
- 公网入口 `ws://81.70.145.148` 与 `/readyz` 已验证，真实双连接通过开房、选色、双骰、选点和走棋。旧服务 PV 保留；两份冷备份归档均完成可读性校验。
- 修复 MySQL 官方入口脚本对数据库名下划线进行转义后造成的 REVOKE 不匹配；生产已应用修正授权 SQL。CI 增加真实 MySQL 初始化/重复执行与账号流程验证，避免同类问题只到部署阶段才暴露。仅文档改动不触发发布。
- 内网 25/100/250/500 个四人房间，全部无错误；最高 2000 连接、60 秒、P95 45.73 ms，Node 平均 CPU 约 0.856 核、RSS 峰值 151.57 MiB。
- 公网 4 房间两分钟 P95 11.92 ms、0 错误；5 房间短测通过；6 房间有显著尾延迟；15 房间在约 2 Mbps 的有效链路下出现 6 次超时。当前建议按 4 房间/16 人活跃容量预留余量，不能将内网连接数当作公网容量。
- 真实数据库 32 个临时账号、并发 4 的注册/登录/新连接恢复通过，测试用户已精确清理。详见 `docs/容量评估.md` 和 `docs/capacity` 原始证据。
- 尚未验证云套餐带宽参数、长时间稳定性、多地区链路或微信 WSS/真机；当前游客会话 Map 无 TTL，游戏仍是单副本内存状态，发布会中断棋局。没有自动把业务房间数封顶为 4，以上为规划建议。

## 最终恢复点（2026-09-09）

- 当前生产镜像：`docker.io/skillludo/server:sha-c3851d72c2c326dcfb8f76a578c7e4adfc2e60a6`；上一个可回退版本为 `2634595`。
- 最终 GitHub 工作流 [34336467561](https://github.com/VoidInHeart/SkillLudo_Server/actions/runs/34336467561) 全部成功：46 项两仓回归、契约检查、真实 MySQL schema 两次执行、账号集成、构建与受限 SSH 发布、公网 revision 检查。
- 最后一次本机公网双连接冒烟返回 `c3851d7` 并通过。旧 3 个 Deployment 与 2 个 StatefulSet 仍为 0 副本；新 MySQL/Node 均 1/1 Running、0 次重启，两个 PVC 为 Bound，无残留压测 Pod。
- 生产环境两项 Secret 与两项 Variable 均已填写，名称见 `docs/部署与运维.md`。本次收尾仅改文档，不触发服务发布；后续恢复以本文件、容量报告和服务器 `/opt/skillludo/releases/current-image` 为准。

## W2：接入公网前端（2026-09-10）

- 客户端仓库已部署前端版本 `38e4cc3` 到 <http://81.70.145.148>，新增独立 Nginx Pod、优先级更高的 Ingress 和附加网络规则；原后端 Ingress 保留用于回退。
- 根路径 WebSocket、`/healthz` 和 `/readyz` 经 Nginx 转发到现有后端，后端镜像仍为 `c3851d7`，Node/MySQL 未重启。公网原冒烟和实际网页双人联机均通过。
- 运维文档已同步网页入口及分仓职责。前端构建/发布脚本在客户端仓库，前端 CI 无需 Secret；本仓 production 的两项 Secret/Variable 不变。
- 新 Nginx 空闲约 1m CPU / 5 MiB。此前容量报告为部署前端前的测量；网页加载与游戏共享公网带宽，不能据此提高推荐人数。

## K1：可撤销选骰与原子移动（2026-09-11）

- 协议提升为 v3，快照增加服务器计算的 `actionOptions`，新增 `COMMIT_MOVE` 一次验证点数方案、rollId 和飞机后再提交；无棋可走时明确确认跳过。
- 旧分步命令保留供现有回归与过渡 AI 使用，客户端新交互不再先锁骰子。未来技能改点复用服务器提供的选项，客户端不自行修改步数。
- 新增 A01–A03：预览隔离、拒绝原子性、过期/越权/伪造输入和一次提交；服务端 37 项及应用/测试类型检查通过。
- 技能设计与已确认口径位于工作区 `../docs/技能规则与实现计划.md`；AI/托管不主动放技能，仅结算被动与已产生的强制效果。下一阶段 K2 实现技能状态和规则，暂不推送或部署协议半成品。

## K2：服务器阵营技能（2026-09-11）

- K1 恢复点 `425684b`。本阶段以 `SkillCatalog` 定义名称/润色文案，`SkillState` 管理觉醒、普通回合/CD、改点选项和能量；替换原空技能占位文件。
- 完成英国换位与缺口绕行、觉醒后 +1/双骰合计；法国 15 秒受击响应、最多两架锁定、清格后 3/4 解锁、逐机减一步无击落救援；中国原始总点数觉醒、强制反向/CD/三级能量强化、被击落回起飞处；美国按敌机数累积连投与五格轰炸。
- 技能操作由服务器验证时机/角色/rollId/目标/次数，法国响应随棋局保存，断线或切换托管默认不锁定。AI 不主动发动技能，强制后续效果与被动仍结算。
- 新增 K01–K12 规则回归；旧 50 局模拟切换到明确的 AI 托管口径，旧虫洞捕获用例更新为美国连投与中国起飞处回归的被动结果。服务端累计 49 项、应用/测试类型检查通过。
- 协议/路径/技能图鉴目录同步到客户端。K3 将接入交互与表现，K4 补真实 WebSocket/浏览器验收；当前仅分阶段本地提交，不推送/部署半成品。

## K4：技能网络回归与配套发布（2026-09-11）

- K2 提交 `084164a`，客户端技能 UI 恢复点 `ad6f2a4`。补上法国受击等待中主动退出/心跳超时进入托管时的即时默认响应，防止不必要地等待 15 秒。
- 新增 `SkillNetwork.test.ts` 5 项真实 WebSocket 用例：原子移动/响应重连与错人/过期/重复拒绝；超时、托管、退出默认不锁定；技能输入格式和托管边界。服务端累计 54 项通过，测试与应用类型检查通过。
- `verify:skill-fixtures` 生成五组客户端表现夹具，不新增生产接口；客户端实际点击/特效验证全部通过，Web/微信构建通过。公网/CI 冒烟改为真实 v3 原子移动或显式跳过。
- README 和工作区测试/规则/恢复文档同步。生产 Secret/Variable 不变；配套客户端 CI ref 与发布结果在收尾记录，旧容量报告不作为技能版容量结论。

## K4 配套发布完成（2026-09-11）

- 服务端 `f430d83` 已推送，CI [34567642123](https://github.com/VoidInHeart/SkillLudo_Server/actions/runs/34567642123) 成功：54 项服务端 + 固定客户端 `3aa8a94` 的 19 项、契约、真实 MySQL 初始化/账号集成、不可变镜像构建和受限 SSH 自动发布全部通过。
- 公网 readyz 返回 `f430d83830980d176d0d5f856182a9f943d7f937`。前端同日配套发布 `3aa8a94`，网页与本机构建预览都完成实际公网双人对局与 v3 原子移动，零浏览器错误；独立 `scripts/smoke.mjs` 最终通过。
- 发布切换期一次就绪读取超时，后续健康与冒烟正常；Node/MySQL/Nginx 均 1/1、0 次重启。数据库未重建，旧业务保持停用，未开展新版容量压测。现有镜像回退记录保留，v3 回退需要前后端成对进行。
- production 已有 Secrets：`DEPLOY_SSH_KEY`、`DEPLOY_KNOWN_HOSTS`；Variables：`DEPLOY_HOST=81.70.145.148`、`DEPLOY_USER=skillludo-deploy`。本次不新增或更换凭据。

## L1：虫洞修正与中国技能调整（2026-09-11）

- 上轮文档收尾 `e4bf5c1` 已推送。新版计划在工作区 `../docs/第二轮规则与恢复计划.md`，后续仍有英国绑定、挂机/暂停/观战/赛后投票及全部客户端联动。
- 虫洞仅结算两端格碰撞，不碰中间普通格；一次行动最多同色跳跃一次，支持先跳再飞、直入后飞再跳，后续跳跃可在落点正常击落。
- 英中改点均无连投；中国觉醒严格超过 100，强化二为三轮未用储备一次、免 CD 且不改变 CD，同一正常回合最多发动一次；强化三为 ±2，CD 保持 3。目录提供随强化等级变化的描述。
- 新增 L01–L04，服务端 58 项及两类 TypeScript 检查通过；同步契约至客户端。尚未发布，后续完整验收后配套更新 CI ref。
